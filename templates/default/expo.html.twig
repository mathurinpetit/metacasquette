{% extends 'base.html.twig' %}
{% block body %}


<h3><a href="/" class="btn btn-lg btn-default"><img class="img-responsive" src="{{ asset('/img/logo/logo_detache_white.png') }}" alt="/MetaCasquette"><h1 class="cover-heading">/META CASQUETTE</h1></a></h3>
<hr/>
<div style="font-size:48px; text-align: justify;">
{{ description | raw}}
</div>
<hr/>
<h1 style="font-size:80px;">Dans cet espace, retrouvez ces modèles : </small></h1>

{% for casquette in casquettes %}
<hr/>
{% set modelNumber = "%04d"|format(casquette[0]) %}
{% set pictureNumber = casquette[8] %}
{% set disponible = casquette[6] %}
<h1 style="font-size:60px;">{{ "#" ~ modelNumber }} - {{ casquette[5] }} <small>({{ casquette[1] }})</small>
   {% if disponible != 'Non disponible' %}
   <span>-&nbsp;{{ disponible | number_format(2) }} €</span>
   <br/>
    <a href="mailto:mathurin@metacasquette.com?subject=[OCTO] Réservation de la MétaCasquette #{{modelNumber}} - {{casquette[5]}}&body=Bonjour MétaCasquette,%0D%0A%0D%0AJ'aimerai réserver la MétaCasquette #{{modelNumber}} - {{casquette[5]}}%0D%0A%0D%0AMa MétaCasquette me sera remise à Octo le 1er Décembre 2023.%0D%0A%0D%0AJe m'engage à verser d'ici le 1er Décembre la somme de {{ disponible | number_format(2) }} € : %0D%0A - Soit en faisant un chèque de {{ disponible | number_format(2) }} € à l'ordre de Mathurin Petit%0D%0A - Soit en virement bancaire (IBAN : XXXXXX) %0D%0A - Soit par Lydia au 06XXXXXX %0D%0A%0D%0AMes coordonnées sont les suivantes : %0D%0A%0D%0ATrès bonne journée%0D%0A" class="btn btn-lg btn-success" style="font-size:60px;">Je réserve</a>

   {% else %}
   <button class="btn btn-lg btn-default"  >{{ disponible }}</button>
   {% endif %}
</h1>
<div class="row">
  <div class="col-sm-10 col-md-10 col-sm-offset-1 col-md-offset-1">
    <div class="row">
      {% if pictureNumber matches '/^\\d+$/' %}
      <div class="col-sm-12 col-md-6 img-mobile">
        <div id="casquette_{{ modelNumber }}_mobile" data-number="{{ pictureNumber }}" data-name="{{ modelNumber }}" class="rotate360mobile" ></div>
        <div class="stage">
          <div class="swipe">
            <i class="fas fa-long-arrow-alt-left fa-3x"></i>
            <i class="fas fa-hand-point-up fa-3x"></i>
            <i class="fas fa-long-arrow-alt-right fa-3x"></i>
          </div>
        </div>
      </div>
      {% endif %}
      <div class="col-sm-12 col-md-6">
        <img src="{{ asset('/img/casquettes/'~modelNumber~'/'~modelNumber~'_side02.jpg') }}" alt="{{ casquette[5] }}" class="img-responsive" />
      </div>
    </div>
  </div>
</div>
<h2>{{ casquette[2] }}<br/><small><i>{{ casquette[3] }}</i></small>&nbsp;</h2>

<h3>Taille : {{ casquette[4] }}</h3>
{% endfor %}
{% endblock %}
